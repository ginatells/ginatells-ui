import styled from 'styled-components'
import { COLOR_PALETTE } from './utils/constants'

export const App = styled.div`
  display: flex;
  flex-direction: column;
  background-color: ${COLOR_PALETTE.MEDIUM_BLACK};
  color: ${COLOR_PALETTE.WHITE};
`
